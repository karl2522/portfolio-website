# Rally Shop Project Card Design

## Goal

Add Rally Shop to the portfolio as a separate project while keeping the existing Rally House project unchanged.

## Placement and presentation

Rally Shop will be the first card in the existing scroll-stack project list, immediately before Rally House. It will reuse the current project-card layout, browser frame, animations, badges, and responsive behavior so it feels native to the portfolio.

## Project content

- Title: `Rally Shop`
- Category: `Personal Project`
- Type: `UI & Frontend Explorations`
- Description: `A polished pickleball storefront concept featuring product filtering, a paddle finder, quick views, and a simulated shopping cart.`
- Technology labels: `HTML`, `CSS`, `JavaScript`
- Demo URL: `https://rally-shop.jaredomen.com/`
- Display domain: `rally-shop.jaredomen.com`
- GitHub link: unavailable, so the existing UI will omit the GitHub button
- Case study: not included

## Image asset

Copy the supplied 1858-by-947 PNG screenshot into `public/images/rallyshop.png` without altering its contents. The card will reference `/images/rallyshop.png` and retain the existing `object-contain` image behavior so the full screenshot remains visible.

## Scope boundaries

No changes will be made to Rally House, other project entries, the shared project-card component design, or global styling.

## Verification

- Run lint and the production build.
- Confirm the Rally Shop card appears first and links to the correct live site.
- Confirm the supplied screenshot renders in the existing desktop and mobile card layouts.
- Confirm Rally House remains present and unchanged.
